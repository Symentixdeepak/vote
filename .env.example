# Local development can run without this file and will use data/votes.json.
# On Vercel, connect a Neon/Postgres Marketplace database or add DATABASE_URL manually.
DATABASE_URL="postgres://USER:PASSWORD@HOST/DATABASE?sslmode=require"
